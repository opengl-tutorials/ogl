#version 330 core

layout(location = 0) in vec3 vertexPosition;
layout(location = 1) in vec3 vertexColor;

out vec3 interpolatedFragmentColor;
in mat4 MVT;

void main(){	
	gl_Position =  MVT * vec4(vertexPosition,1);
	interpolatedFragmentColor = vertexColor;
}

